#pragma once
#include "stdafx.h"

typedef struct game {
	/* This struct should have everything you need to run,load or save the game
		- SnakeHead
		// Complement this Comment if needed
	*/
	SnakeHead snkHead;
}Game;